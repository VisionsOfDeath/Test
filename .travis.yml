language: python
python:
  - "3.5"

install:
  - "pip install -r requirements.txt"
  - "pip install coveralls"

script: py.test test --cov=gather --cov=lib --cov-report term-missing

after_success:
  - coveralls
